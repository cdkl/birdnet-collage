services:
  birdnet-collage:
    build: .
    container_name: birdnet-collage
    ports:
      - "8081:8081"
    environment:
      - BIRDNET_GO_URL=${BIRDNET_GO_URL}
      - DEBUG=false
    restart: unless-stopped